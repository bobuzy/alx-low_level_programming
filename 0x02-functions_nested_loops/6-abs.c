#include "main.h"

/**
 * _abs - Return the absolute value of a number
 * @n: The number to be checked
 *
 * Return: The absolute value
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
